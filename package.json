{
  "name": "k8s-nestjs",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "private": "true",
  "scripts": {
    "start": "lerna run --parallel start",
    "build:backend":"yarn workspace server build",
    "build:frontend": "yarn build --workspace packages/frontend"
  },
  "devDependencies": {
    "lerna": "^8.1.9",
    "typescript": "^5.7.2"
  }
}